{
  "name": "@codegateway/demo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/client/index.ts --outdir=dist --minify"
  },
  "dependencies": {
    "@codegateway/core": "workspace:*",
    "@codegateway/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
